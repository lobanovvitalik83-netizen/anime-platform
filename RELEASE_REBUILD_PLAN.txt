RELEASE REBUILD PLAN

This archive is Stage 1 foundation rebuild based on current repository state.
Applied fixes:
- Telegram report BIGINT + router order + report handler isolation
- UI stabilization for admin dashboard, sidebar, chats, notifications, admin actions
- current repository files preserved without deleting existing modules

Next stages to finish release:
Stage 2:
- achievements with images, grant/revoke, profile badges
- unify settings pages and permissions visibility
- card import/export rewritten around card payloads
- Yandex-only media flow hardening
- employee pages redesign

Stage 3:
- final product UI polish, role-based navigation cleanup
- production hardening, DB cleanup routines, final QA pass
